// Disable SSR for the entire application
export const ssr = false;

// Enable client-side routing
export const csr = true;

// Prerender can be disabled if you don't want any prerendering
export const prerender = false;
